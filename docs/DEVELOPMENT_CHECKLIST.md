# Development Checklist

## ⚠️ ВАЖНО: Используйте этот чеклист ТОЛЬКО после подтверждения пользователя, что функциональность работает корректно!

---

## 1. Синхронизация 2D ↔ 3D

### При добавлении новых параметров битов или операций:

**Проверить сигнатуру в ThreeModule.js:**
```javascript
buildPanelBitsSignature(width, height, thickness, bits = [], panelAnchor) {
    const bitSig = bits.map((b) => ({
        // ✅ Добавить новый параметр здесь
        newParameter: b.newParameter || defaultValue,
    }));
}
```

**Параметры, которые ОБЯЗАТЕЛЬНО должны быть в сигнатуре:**
- `x`, `y` - позиция бита
- `operation` - тип операции (AL, OU, IN, VC, PO)
- `profile` - профиль инструмента
- `pocketOffset` - для PO операций
- **ЛЮБОЙ параметр, изменение которого должно вызывать перестроение 3D**

**Где используется:**
- `ThreeModule.js` → `buildPanelBitsSignature()` - построение сигнатуры
- `ThreeModule.js` → `updatePanel()` - проверка сигнатуры перед rebuild

---

## 2. Logging и отладка

### При создании нового модуля:

**Добавить logger в начало файла:**
```javascript
import LoggerFactory from "../core/LoggerFactory.js";

const log = LoggerFactory.createLogger("ModuleName");
```

**Использовать уровни логирования:**
```javascript
log.debug("Детальная информация для отладки");
log.info("Общая информация о ходе работы");
log.warn("Предупреждения о потенциальных проблемах");
log.error("Критические ошибки", errorObject);
```

**Важные места для логирования:**
- Начало/конец выполнения функций (info)
- Изменение состояния (info)
- Валидация данных (warn при проблемах)
- Catch блоки (error с полным stack trace)
- Условные ветвления с важной логикой (debug)

**Где находится:**
- `src/core/LoggerFactory.js` - фабрика логгеров
- Настройки уровней в консоли: `LoggerFactory.setLevel("ModuleName", "debug")`

---

## 3. Документация кода (JSDoc)

### Обязательные JSDoc для всех публичных методов:

```javascript
/**
 * Краткое описание функции (одна строка)
 * 
 * Подробное описание того, что делает функция,
 * какие сайд-эффекты имеет, что важно знать.
 * 
 * @param {Type} paramName - Описание параметра
 * @param {Type} [optionalParam=default] - Опциональный параметр
 * @returns {Type} Описание возвращаемого значения
 * @throws {ErrorType} Когда выбрасывается исключение
 * 
 * @example
 * // Пример использования
 * functionName(param1, param2);
 */
```

**Важно документировать:**
- Параметры с их типами и значениями по умолчанию
- Возвращаемые значения
- Побочные эффекты (изменение DOM, глобального состояния)
- Примеры использования для сложных функций
- Связь с другими модулями

---

## 4. Модификация путей (Path Modifier)

### При добавлении новых операций с offset путей:

**Проверить поддержку в extrudeAlongPath:**
```javascript
// Для mitered и round экструзий
extrudeAlongPath(profile, path, color, zOffset, type, side, options, pathModifier)

// pathModifier: { offset: number, cornerStyle: 'round'|'bevel'|'miter' }
```

**Убедиться что:**
1. ✅ Paper.js offset применяется в `_modifySVGPathWithOffset`
2. ✅ Результат проходит через `approximatePath` (Bezier → арки)
3. ✅ SVG path корректно парсится в `parsePathToCurves`
4. ✅ 2D кривые конвертируются в 3D в `createCurveFromCurves`
5. ✅ Группировка работает в `groupCurves`
6. ✅ Экструзия обрабатывает группы в `_extrudeMitered` / `_extrudeRound`

**Файлы:**
- `src/three/ExtrusionBuilder.js` - основная логика
- `src/utils/arcApproximation.js` - Bezier → Arc conversion
- `src/operations/PaperOffsetProcessor.js` - Paper.js offset wrapper

---

## 5. Обновление документации проекта

### После добавления новой функциональности:

**Обновить файлы документации:**
- `docs/API_REFERENCE.md` - новые публичные API
- `docs/ARCHITECTURE.md` - изменения в архитектуре
- `docs/CHANGELOG.md` - что добавлено/изменено/исправлено
- `docs/TODO.md` - убрать выполненные задачи

**Шаблон для CHANGELOG.md:**
```markdown
## [YYYY-MM-DD] - Feature Name

### Added
- Новая функциональность X
- Поддержка параметра Y

### Changed
- Изменено поведение Z для улучшения производительности

### Fixed
- Исправлена ошибка с W при условии Q
```

---

## 6. Координаты и трансформации

### При работе с 2D → 3D конвертацией:

**Важные системы координат:**
1. **SVG Canvas** (2D) - Y вниз, origin в top-left
2. **Paper.js** (2D) - Y вниз, origin в top-left  
3. **Three.js** (3D) - Y вверх, origin в центре панели

**Функции конвертации:**
```javascript
// ExtrusionBuilder.js
convertPoint2DTo3D(x2d, y2d, partFrontX, partFrontY, ...)

// Учитывать:
// - panelAnchor (top-left / bottom-left)
// - depth (глубина в материал)
// - panelThickness
```

**Проверять при изменениях:**
- ✅ Инверсия Y координат
- ✅ Смещение origin
- ✅ Z координата для top/bottom side
- ✅ Правильность bounding box

---

## 7. Экструзия и геометрия

### Типы экструзии:

**Mitered (острые углы):**
- Для VC операций
- Использует `ProfiledContourGeometry`
- Путь объединяется в один непрерывный
- Требует 3D CurvePath или SVG с трансформацией

**Round (скругленные углы):**
- Для AL, OU, IN, PO операций
- Использует группировку сегментов
- Разные алгоритмы для LINE/ARC/BEZIER групп
- Lathe заполнение углов между группами

**При добавлении нового типа:**
1. Создать `_extrudeNewType()` метод
2. Добавить обработку в `extrudeAlongPath()`
3. Поддержать SVG path string + CurvePath
4. Передать `options` для координатных трансформаций

---

## 8. Тестирование изменений

### Минимальный чеклист перед коммитом:

**2D Canvas:**
- [ ] Биты корректно отображаются
- [ ] Offset контуры правильные
- [ ] Phantom биты на правильных позициях
- [ ] Drag & drop работает
- [ ] Input поля обновляют визуализацию

**3D View:**
- [ ] Геометрия соответствует 2D
- [ ] Offset пути применены корректно
- [ ] CSG операции работают (Part view)
- [ ] Material/edges переключаются
- [ ] Export в STL успешен

**Производительность:**
- [ ] Нет лагов при drag & drop
- [ ] Перестроение 3D не блокирует UI
- [ ] Сигнатура предотвращает лишние rebuilds
- [ ] Логи не спамят в production

---

## 9. Обработка ошибок

### Паттерн обработки ошибок:

```javascript
try {
    // Main logic
    
    if (!validation) {
        this.log.warn("Validation failed:", details);
        return fallbackValue;
    }
    
    // Continue...
    
} catch (error) {
    this.log.error("Operation failed:", error.message, {
        context: relevantData,
        stack: error.stack
    });
    return fallbackValue; // Всегда возвращать безопасное значение
}
```

**Важно:**
- ✅ Всегда логировать с контекстом
- ✅ Возвращать безопасные fallback значения
- ✅ Не прерывать работу всего приложения
- ✅ Показывать понятные сообщения в UI (при необходимости)

---

## 10. Оптимизация производительности

### Критические места:

**Кеширование:**
- ThreeModule сигнатура предотвращает rebuilds
- Paper.js paths кешируются между вызовами
- Geometry disposal при удалении meshes

**Проверять при изменениях:**
- [ ] Нет утечек памяти (dispose geometry/material)
- [ ] Сигнатура включает ВСЕ релевантные параметры
- [ ] Асинхронные операции не блокируют UI
- [ ] Debounce для input событий

**Профилирование:**
```javascript
console.time("operationName");
// ... код
console.timeEnd("operationName");
```

---

## Финальный чеклист перед завершением задачи

- [ ] ✅ **Функциональность работает** - подтверждено пользователем
- [ ] 🔍 Сигнатуры обновлены (если добавлены параметры)
- [ ] 📝 Logger добавлен в новые модули
- [ ] 📚 JSDoc добавлен для публичных методов
- [ ] 📖 Документация обновлена (API, CHANGELOG, TODO)
- [ ] 🧪 Тестирование пройдено (2D, 3D, производительность)
- [ ] 🐛 Обработка ошибок реализована
- [ ] ⚡ Производительность проверена (нет утечек)

---

**Последнее обновление:** 2026-01-12  
**Версия:** 1.0
