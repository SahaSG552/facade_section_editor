при определенном направлении пути, некоторые меши строится с инвертированными нормалями. Я хочу, чтобы ты изучил пайплайн и сделал направление нормалей всегда наружу и независящим от winding order. унифицировать текущий пайплайн. в CСW сейчас всё правильно. в CW варианте инвертированы inner extrusion , в extensions - lathes и inner extrusion инвертированы. пожалуйста, будь внимателен и не поломай то, что работает #extrusion #offset (A) {cm:2026-05-07}
Нужна полная синхронизация 2d канваса и 3д, любые изменения, например изменение контура детали, и соответственно оффсетов - нужно сразу пересчитывать изменяемые элементы в 3д
Положение Part Front не зафиксировано - при изменении размера экрана она может смещаться и при переключении на Edit режим тоже может произойти смещение
Нужно сделать устойчивый алгоритм объединения самопересекающегося меша #extrusion

BUG: Replicad CSG не вычитает AL/PO при offset=0 в части пользовательских сцен (визуально совпадающие поверхности остаются после cut). Текущий workaround в коде: path-level outset -0.01 для CSG+offset≈0, но кейс остается нестабильным. Нужна детерминированная стратегия anti-coincident для sweep cutter + test fixture реального кейса из UI (step-by-step repro, expected boolean result, trace выбранного spine candidate). #replicad #csg #offset (A) {cm:2026-06-02}